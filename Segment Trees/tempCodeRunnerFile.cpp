ll n;
    // cin >> n;
    // vil v(n);
    // for (ll i = 0; i < n; i++)
    //       cin >> v[i];
    // vil segTree(4 * n + 1);
    // build_Tree(1, 0, n - 1, v, segTree);
    // debug(segTree);
    // ll q;
    // cin >> q;
    // while (q--)
    // {
    //       ll l, r;
    //       cin >> l >> r;
    //       cout << query(1, 0, n - 1, l, r, segTree);
    //     }